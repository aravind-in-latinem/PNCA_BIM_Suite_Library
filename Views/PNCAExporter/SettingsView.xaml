<UserControl x:Class="PNCA_BIM_Suite_Library.Views.PNCAExporter.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PNCA_BIM_Suite_Library.Views.PNCAExporter"
             mc:Ignorable="d" 
             d:DesignHeight="950" d:DesignWidth="800">
    <Grid Background="White">
        <ScrollViewer VerticalScrollBarVisibility="Visible"
                      HorizontalScrollBarVisibility="Disabled"
                      Margin="10">
            <StackPanel>
                <local:PDFSettingsView DataContext="{Binding PDFSettingsViewModel}"/>
                <local:DWGSettingsView DataContext="{Binding DWGSettingsViewModel}"/>
                <local:DWFSettingsView DataContext="{Binding DWFSettingsViewModel}" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
